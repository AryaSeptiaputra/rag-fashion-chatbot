# Dependency penilaian mutu retrieval (DeepEval).
#
# Dipasang di venv TERPISAH, bukan di venv utama:
#     python -m venv .venv-eval
#     .venv-eval\Scripts\pip install -r requirements-eval.txt
#
# DeepEval menarik grpcio, opentelemetry, dan posthog. Meresolusi semua itu
# terhadap pin llama-index, chromadb, dan streamlit yang sekarang bekerja adalah
# risiko yang tidak perlu diambil demi kenyamanan satu venv.

-r requirements.txt

# Project dipasang sebagai package supaya app.evals bisa di-import dari
# notebook dan script.
-e .

# Penilai
deepeval==4.2.1

# Model juri. Ini satu-satunya bagian project yang menyentuh API berbayar, dan
# hanya harness eval yang memakainya -- chatbot-nya tetap berjalan penuh di
# lokal tanpa kunci API.
#
# Ditahan di 0.x dengan sengaja: anthropic 1.x pindah dari httpx ke httpx2, dan
# menariknya kembali ke venv ini akan menghadirkan dua pustaka HTTP berdampingan
# tanpa alasan. 0.93.0 memakai httpx<1, jadi httpx==0.28.1 di requirements.txt
# tetap yang terpakai.
anthropic==0.93.0

# Tabel ringkasan dan grafik di notebook.
pandas==3.0.5
matplotlib==3.11.1
