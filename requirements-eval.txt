# Dependency penilaian mutu (RAGAS + DeepEval).
#
# Dipasang di venv TERPISAH, bukan di venv utama:
#     python -m venv .venv-eval
#     .venv-eval\Scripts\pip install -r requirements-eval.txt
#
# Alasannya, RAGAS menarik langchain, langchain-community, dan datasets>=4.0.0,
# sedangkan DeepEval menarik grpcio, opentelemetry, dan posthog. Meresolusi
# semua itu terhadap pin llama-index, chromadb, dan streamlit yang sekarang
# bekerja adalah risiko yang tidak perlu diambil demi kenyamanan satu venv.

-r requirements.txt

# Project dipasang sebagai package supaya app.evals bisa di-import dari
# notebook dan script.
-e .

# Penilai
ragas==0.4.3
deepeval==4.2.1

# Transport HTTP ke endpoint kompatibel OpenAI milik Ollama -- bukan akun
# OpenAI. RAGAS 0.4 menghapus wrapper LangChain/LlamaIndex dan menuntut client
# yang sudah jadi, jadi paket ini dipakai langsung di app/evals/judge.py.
#
# Ditahan di 2.x dengan sengaja: instructor, yang dipakai RAGAS untuk keluaran
# terstruktur, menuntut openai<3.0.0. Menaikkan ke 3.x akan menggagalkan
# resolusi.
openai==2.54.0

# Grafik ringkasan di notebook. pandas tidak perlu didaftarkan; ia sudah ikut
# lewat datasets milik RAGAS, dan itu versi yang sama dipakai result.to_pandas().
matplotlib==3.11.1
